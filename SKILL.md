---
name: recall-compact
description: 与 Recall 配合，在可恢复阶段边界按需整理交接并调用当前环境的上下文切换能力，随后恢复任务。适用于长任务、明确压缩请求及已配置的 Recall 默认联用；短任务只评估，不强制压缩。
---

# Recall Compact

帮助长任务在上下文切换后继续。开始使用时告知正在应用本技能。本技能已加载时不要重复加载或递归调用。

## 与 Recall 配合

先使用当前安装的 Recall，遵循消费项目的现行规则与读取顺序。若由 Recall 的联用入口进入，沿用本轮已读上下文；若独立进入，定位并读取 Recall 技能。Recall 不可用时说明缺失，不能冒充完成 Recall 恢复；明确压缩请求仍可按当前环境能力处理。

本技能的权威规则为 [根规则](logic_readme.md#当前制度) 与 [流程规则](logic_domains/compact/logic_readme.md#当前制度)，首次应用时读取这两个规则段即可；开发本技能时才读完整项目文档。不要把附属技能仓库当作当前业务项目根目录。

## 阶段检查与执行

1. 判断是否值得切换（RULE-010）：用户明确要求；或者阶段已有可复核成果，且运行时显示上下文压力、旧探索内容占比明显较大或下一阶段转向其他领域。短任务、只完成一小步、刚恢复且无新进度时继续工作。精确占用不可获取时用定性依据，不捏造百分比；1M 配置不代表真实可用容量。
2. 核对安全接续点（RULE-011/012）：重要结果及下一步有可恢复来源；写入或外部操作已明确完成、失败或保留可查询句柄。对于结果未明的部署、支付、推送等操作，先查状态或记录“待核实，勿重放”。不要为了压缩提交、晋升或关闭尚未完成的 CHG。
3. 整理最小交接：按 Recall 将应记录的项目事实更新到所属账本；再用当前环境支持、确实能跨切换读取的交接载体保留执行状态。需要提纲时读 [handoff](references/handoff.md)。禁止把真实交接存进本技能仓库、技能安装目录或公开文档；没有可靠载体时暂缓切换。保留已启用本技能、触发原因与防循环状态。
4. 检查实际工具：只调用当前工具列表里有说明的上下文切换或压缩能力；当前环境若提供 `functions.new_context`，可用它切换上下文，它不保证采用某种特定压缩算法。其他环境按其文档操作，不能将 CLI 文本命令当作工具调用，也不能用普通脚本模拟成功。没有能力时报告“已整理交接，未执行上下文切换”，继续可完成的工作。
5. 调用前简短通知用户。执行后分别确认：交接已保存、调用已发起、实际切换已确认、恢复已完成。调用返回不明或无可核实切换证据，只报已请求或未确认，不循环重试。
6. 恢复后：检查目标、来源、授权边界与剩余动作；亲自按 Recall 重读根 readme/change 和 route 命中领域；历史及代码按下一步需要读取。核对运行中任务与文件状态，避免重复执行外部动作。摘要与现行规则冲突时核实来源，保留未决裁决门；不要要求用户重复已有明确授权。
7. 有明确下一步就继续；没有下一步则交付。恢复完成不等于业务任务完成，不因成功切换宣称测试通过或发布成功。

## 按需资料

- 设计、取舍、默认联用和环境限制：[design](references/design.md)。
- 初次安装、状态检查、撤销：[README](README.md#安装与默认联用)。
- 验收与证据口径：[acceptance](references/acceptance.md)。
